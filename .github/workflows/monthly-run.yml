name: GEE Full Pipeline After Export

on:
  workflow_dispatch:

jobs:
  poll-convert-clean:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r gee-pipeline/requirements.txt

    - name: Create service-key.json
      env:
        SERVICE_KEY_JSON: ${{ secrets.SERVICE_KEY_JSON }}
      run: |
        echo "$SERVICE_KEY_JSON" > gee-pipeline/service-key.json

    - name: DEBUG — Show generated JSON
      run: |
        echo "----- JSON START -----"
        cat gee-pipeline/service-key.json
        echo "----- JSON END -----"

    # --------------------------------------------------------
    # 🔐 Authenticate
    # --------------------------------------------------------
    - name: Authenticate Service Account
      run: |
        export GOOGLE_APPLICATION_CREDENTIALS=gee-pipeline/service-key.json
        gcloud auth activate-service-account --key-file=gee-pipeline/service-key.json

    # --------------------------------------------------------
    # 📥 Poll + Download + Convert
    # --------------------------------------------------------
    # - name: Download from GCS & Convert to Parquet
    #   env:
    #     GOOGLE_APPLICATION_CREDENTIALS: gee-pipeline/service-key.json
    #     GCS_BUCKET: ${{ secrets.GCS_BUCKET }}
    #   run: |
    #     python gee-pipeline/scripts/poll_download_convert.py

    # - name: DEBUG — list files after convert
    #   run: |
    #     echo "Listing gee-pipeline/outputs/raw_parquet"
    #     ls -R gee-pipeline/outputs/raw_parquet || echo "not found"

    #     echo "Listing local_raw"
    #     ls -R local_raw || echo "not found"

    # --------------------------------------------------------
    # 🧪 DEBUG bucket structure
    # --------------------------------------------------------
    # - name: DEBUG — Check poll script prefix logic
    #   env:
    #     GCS_BUCKET: ${{ secrets.GCS_BUCKET }}
    #   run: |
    #     echo "🔍 Checking bucket file layout..."
    #     gsutil ls gs://${GCS_BUCKET}/raw_export/** || echo "No files found in raw_export"
        
    #     echo "🔍 Checking NDVI directory"
    #     gsutil ls gs://${GCS_BUCKET}/raw_export/NDVI/ || echo "No NDVI"

    #     echo "🔍 Checking SoilMoisture directory"
    #     gsutil ls gs://${GCS_BUCKET}/raw_export/SoilMoisture/ || echo "No SoilMoisture"

    #     echo "🔍 Checking FireCount directory"
    #     gsutil ls gs://${GCS_BUCKET}/raw_export/FireCount/ || echo "No FireCount"

    #     echo "🔍 Checking LST directory"
    #     gsutil ls gs://${GCS_BUCKET}/raw_export/LST/ || echo "No LST"

    # --------------------------------------------------------
    # 🧹 Clean parquet
    # --------------------------------------------------------
    - name: Clean raw parquet
      run: |
        python gee-pipeline/scripts/clean_raw_data.py

    # --------------------------------------------------------
    # 🔗 Merge all cleaned parquet → 1 file
    # --------------------------------------------------------
    - name: Merge cleaned parquet
      run: |
        python gee-pipeline/scripts/merge_cleaned.py

    - name: DEBUG — Check merged output
      run: |
        ls -lh gee-pipeline/outputs/merged

    # --------------------------------------------------------
    # 💾 Commit cleaned + merged data
    # --------------------------------------------------------
    - name: Commit Cleaned + Merged Data to Repo
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"
        git add gee-pipeline/outputs/clean/*
        git add gee-pipeline/outputs/merged/*
        git commit -m "Automated clean + merge update" || echo "No changes to commit"
        git push

