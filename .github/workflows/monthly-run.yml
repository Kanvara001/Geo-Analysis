name: GEE Full Pipeline After Export

on:
  workflow_dispatch:

jobs:
  poll-convert-clean:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r gee-pipeline/requirements.txt

    - name: Create service-key.json
      env:
        SERVICE_KEY_JSON: ${{ secrets.SERVICE_KEY_JSON }}
      run: |
        echo "$SERVICE_KEY_JSON" > gee-pipeline/service-key.json

    - name: DEBUG — Show generated JSON
      run: |
        echo "----- JSON START -----"
        cat gee-pipeline/service-key.json
        echo "----- JSON END -----"


    # --------------------------------------------------------
    # 🔐 Authenticate — FIXED version (no project switch)
    # --------------------------------------------------------
    - name: Authenticate Service Account (No Project Switching)
      run: |
        export GOOGLE_APPLICATION_CREDENTIALS=gee-pipeline/service-key.json
        gcloud auth activate-service-account --key-file=gee-pipeline/service-key.json


    # --------------------------------------------------------
    # ✅ Run Export Script (GEE → GCS)
    # --------------------------------------------------------
    - name: Run Export Script (GEE to GCS)
      env:
        SERVICE_ACCOUNT: ${{ secrets.SERVICE_ACCOUNT }}
        GCS_BUCKET:     ${{ secrets.GCS_BUCKET }}
        GOOGLE_APPLICATION_CREDENTIALS: gee-pipeline/service-key.json
      run: |
        python gee-pipeline/scripts/gee_export_tasks.py --var FireCount

    # --------------------------------------------------------
    # ✅ Poll + Download + Convert 
    # --------------------------------------------------------
    - name: Download from GCS & Convert to Parquet
      env:
        GCS_BUCKET: ${{ secrets.GCS_BUCKET }}
        run: |
          python gee-pipeline/scripts/poll_download_convert.py --var FireCount

    # --------------------------------------------------------
    # 📥 DOWNLOAD parquet จาก GCS
    # --------------------------------------------------------
    - name: Download parquet from GCS
      env:
        GCS_BUCKET: ${{ secrets.GCS_BUCKET }}
      run: |
        echo "📥 Downloading parquet from GCS/parquet/"
        mkdir -p gee-pipeline/outputs/raw_parquet
        gsutil -m cp -r gs://${GCS_BUCKET}/parquet/* gee-pipeline/outputs/raw_parquet/

    - name: DEBUG — list raw_parquet
      run: |
        ls -R gee-pipeline/outputs/raw_parquet

    # --------------------------------------------------------
    # 🧹 Clean raw parquet
    # --------------------------------------------------------
    - name: Clean raw parquet
      run: |
        python gee-pipeline/scripts/clean_raw_data.py

    # --------------------------------------------------------
    # 💾 COMMIT CLEAN (สำคัญมาก)
    # --------------------------------------------------------
    - name: Commit CLEAN parquet
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"
        git add gee-pipeline/outputs/clean/*
        git commit -m "Automated clean parquet update" || echo "No clean changes to commit"
        git push

    # --------------------------------------------------------
    # 🧩 FILL missing data (หลัง clean)
    # --------------------------------------------------------
    - name: Fill missing monthly data
      run: |
        python gee-pipeline/scripts/fill_missing_after_clean.py

    # --------------------------------------------------------
    # 💾 COMMIT FILLED parquet
    # --------------------------------------------------------
    - name: Commit FILLED parquet
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"
        git add gee-pipeline/outputs/clean/*
        git commit -m "Automated FILL missing data" || echo "No fill changes"
        git push

    # --------------------------------------------------------
    # 🔗 Merge cleaned parquet (หลัง clean ถูก commit แล้ว)
    # --------------------------------------------------------
    - name: Merge cleaned parquet
      run: |
        python gee-pipeline/scripts/merge_cleaned.py

    - name: DEBUG — Check merged output
      run: |
        ls -lh gee-pipeline/outputs/merged

    # --------------------------------------------------------
    # 💾 COMMIT MERGED
    # --------------------------------------------------------
    - name: Commit MERGED parquet
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"
        git add gee-pipeline/outputs/merged/*
        git commit -m "Automated merged parquet update" || echo "No merged changes to commit"
        git push
